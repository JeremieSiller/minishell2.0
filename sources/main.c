#include <minishell.h>
#include <parser.h>
#include <readline/readline.h>

int	main(int argc, char *argv[], char **env)
{

	char *str = readline(">");

	// t_cmds *list = parse(str);



	// while (list)
	// {
		
	// 	list = list->next;
	// }

	return (0);
	(void)argc;
	(void)argv;
	(void)env;
	(void)str;
}